fn main: () -> () {
    while @(false) {
        while @(false) {
            if @(false) {
                continue;
            }
            while @(false) {
                while @(false) {
                        if @(false) {
                            @(321) @pop
                        } else {}
                    }
                }
            }
    }
}
