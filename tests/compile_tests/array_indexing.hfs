//? COMPILE
fn print: (i32) -> () { @pop; }

fn main: () -> () {
    let arr: [5]i32;
    let i: i32;
    @(0) &= i;
    while @(i 5 <) {
        @(i 100 * i) [&]= arr;
        @(i 1 +) &= i;
    }
    @(0) &= i;
    while @(i 5 <) {
        @(arr i []) &> print;
        @(i 1 +) &= i;
    }
}
