use crate::hfs::{ast::*, Literal};
// here is where youll create the CFG pass and the new IR generation

struct InstArena {
    // same exact idea as ASTArena
}
struct InstBuilder {
    // similar to StackAnalyzer, pretty much that 
}
