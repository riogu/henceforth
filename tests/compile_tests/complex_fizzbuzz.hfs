

fn fizz_buzz: (i32 str) -> (str i32) {
    let var: str; &= var;
    if @(15 % 0 ==) {
        @("fizzbuzz" 213)
    } else if @(3 % 0 ==) {
        @("fizz" 420)
    } else if @(5 % 0 ==) {
        @("buzz" 69)
    } else {
        @pop
        @("no fizzbuzz" 42069)
    }
}

fn foo: () -> (str i32) {
    @(420 "bar") &> fizz_buzz;
}

fn main: () -> () {
    @() :> foo;
    @pop;
    @pop;
}

